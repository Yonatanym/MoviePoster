package com.example.poster;

public class Poster {

    // Declare member variables for the poster details
    String name, createdBy, story;    // The name of the movie or poster, A brief description, 
    int image;   // The resource ID of the image (poster) associated with  
    Boolean isSelected= false;  // Boolean flag to track if the poster is selected or not  
    float rating;     // The rating of the movie, represented as a float 

}
